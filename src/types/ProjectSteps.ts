export interface ProjectStep {
       step_id: string;
       project_id: string;
       name: string;
       description: string;
       status: string;
       start_date: string;
       end_date: string;
      // Add any other relevant fields for a project step
    }
